Utilizar comando *make* para compilar, se generará el archivo *a.out*.
Usar "./a.out [fichero aleatorios]" para ejecutar el programa.

El fichero "distancias_10.txt" debe estar en la misma carpeta que el ejecutable.
